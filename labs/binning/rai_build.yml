rai:
  version: 0.2
  image: pumps2018:amd64-cuda100
resources:
  cpu:
    architecture: ppc64le
commands:
  build:
    - cmake /src
    - make
    # - ./binning -l
    # - ./binning -h
    # - ./binning 'CPUNormal*' -a
    #- ./binning 'CPUNormal*' -a
    - ./binning -a
